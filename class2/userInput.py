# User input
# By default, input is taken as string
name = input("Enter your name:")
print(name)

# If we want to take integer as input, we need to typecast the user input with integer value
age = int(input("Enter your age:"))
print(type(age))
